DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=
DATABASE_PASSWORD=

# Site-wide HTTP basic auth gate (pre-launch). Auth is enforced only when
# HTTP_AUTH_USER is set; leave both empty in development.
HTTP_AUTH_USER=
HTTP_AUTH_PASSWORD=
