<% content_for(:title) { t("owner.wine_lists.new_title") } %>

<header class="admin-head">
  <h1 class="admin-title"><%= t("owner.wine_lists.new_title") %></h1>
  <span class="admin-subtitle"><%= @restaurant.name %></span>
</header>

<%= render "form", wine_list: @wine_list %>
