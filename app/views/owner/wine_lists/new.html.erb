<% content_for(:title) { t("owner.wine_lists.new_title") } %>

<div class="mb-8">
  <h1 class="font-display text-3xl font-light"><%= t("owner.wine_lists.new_title") %></h1>
</div>

<%= render "form", wine_list: @wine_list %>
