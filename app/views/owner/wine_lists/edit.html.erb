<% content_for(:title) { t("owner.wine_lists.edit_title") } %>

<header class="admin-head">
  <h1 class="admin-title"><%= t("owner.wine_lists.edit_title") %></h1>
  <span class="admin-subtitle"><%= @wine_list.name %></span>
</header>

<%= render "form", wine_list: @wine_list %>

<hr class="rule-section my-10">

<%= render "members", wine_list: @wine_list %>
