<% content_for(:title) { t("owner.wine_lists.edit_title") } %>

<div class="mb-8">
  <a href="<%= owner_restaurant_wine_lists_path(@restaurant) %>" class="text-sm text-base-content/50 hover:text-primary flex items-center gap-1 mb-4">
    <%= icon "arrow-left", css: "size-3.5" %>
    <%= t("owner.wine_lists.title") %>
  </a>
  <h1 class="font-display text-3xl font-light"><%= t("owner.wine_lists.edit_title") %></h1>
</div>

<%= render "form", wine_list: @wine_list %>

<div class="divider-wine my-10"></div>

<%= render "members", wine_list: @wine_list %>
