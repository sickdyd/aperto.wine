<% content_for(:title) { t("owner.tables.edit_title") } %>

<div class="admin-head">
  <h1 class="admin-title"><%= t("owner.tables.edit_title") %></h1>
  <p class="admin-subtitle"><%= @table.name %></p>
</div>

<%= render "form", table: @table %>
