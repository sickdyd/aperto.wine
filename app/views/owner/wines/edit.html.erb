<% content_for(:title) { t("owner.wines.edit_title") } %>

<a href="<%= owner_restaurant_wines_path(@restaurant) %>" class="link-mono gap-2">
  <%= icon "arrow-left", css: "size-3.5" %>
  <%= t("owner.wines.title") %>
</a>

<div class="admin-head mt-2">
  <h1 class="admin-title"><%= t("owner.wines.edit_title") %></h1>
  <p class="admin-subtitle"><%= @wine.name %></p>
</div>

<%= render "form", wine: @wine %>
