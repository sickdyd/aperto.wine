<% content_for(:title) { t("owner.restaurants.edit_title") } %>

<div class="mb-8">
  <a href="<%= owner_restaurant_path(@restaurant) %>" class="text-sm text-base-content/50 hover:text-primary flex items-center gap-1 mb-4">
    <%= icon "arrow-left", css: "size-3.5" %>
    <%= t("shared.back") %>
  </a>
  <h1 class="font-display text-3xl font-light"><%= t("owner.restaurants.edit_title") %></h1>
</div>

<%= render "form", restaurant: @restaurant %>
