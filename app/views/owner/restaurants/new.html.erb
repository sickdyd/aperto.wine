<% content_for(:title) { t("owner.restaurants.new_title") } %>

<div class="mb-8">
  <h1 class="font-display text-3xl font-light"><%= t("owner.restaurants.new_title") %></h1>
</div>

<%= render "form", restaurant: @restaurant %>
