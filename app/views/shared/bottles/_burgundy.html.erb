<%# Burgundy: wider base, gentle sloping shoulders, longer neck %>
<%# Flat minimal: currentColor outline + flat liquid fill, no decoration %>
<%# Locals: fill_percent (0-100), sealed (boolean), wine_color (hex), bottle_id (unique id) %>
<% outline = "M 32,22 L 32,50 C 32,58 28,68 24,78 C 17,90 15,96 15,102 L 15,184 Q 15,191 22,191 L 58,191 Q 65,191 65,184 L 65,102 C 65,96 63,90 56,78 C 52,68 48,58 48,50 L 48,22 Z" %>
<svg viewBox="0 0 80 200" xmlns="http://www.w3.org/2000/svg" class="bottle-svg" aria-hidden="true">
  <defs>
    <clipPath id="clip-<%= bottle_id %>">
      <path d="<%= outline %>" />
    </clipPath>
  </defs>

  <% if fill_percent > 0 %>
    <% liquid_top = 184 - (fill_percent / 100.0 * 100) %>
    <rect x="0" y="<%= liquid_top %>" width="80" height="<%= 200 - liquid_top %>"
      fill="<%= wine_color %>" clip-path="url(#clip-<%= bottle_id %>)" />
  <% end %>

  <path d="<%= outline %>" fill="currentColor" fill-opacity="0.05"
    stroke="currentColor" stroke-width="2" stroke-linejoin="round" />

  <% if sealed %>
    <rect x="33.5" y="9" width="13" height="14" rx="1.5" fill="currentColor" opacity="0.35" />
  <% end %>
</svg>
